# BML+ Mod Templates

This directory contains author-facing starting points.

- `native-mod-template`: CMake-based native `.bmodp` mod template.
- `script-mod-template`: AngelScript `*.mod.as` script mod template.
