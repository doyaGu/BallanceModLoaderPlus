// Generated by tools/interop_codegen.py. Do not edit by hand.
#pragma once

#include "BML/InteropApi.h"

/*
 * C++ exposes header-only namespaced convenience constants below.
 * C receives the same descriptor through static C data and macros in
 * the #else branch. Neither form exports a C++ symbol from the DLL.
 */
#ifdef __cplusplus
#include <cstdint>
#include "BML/InteropClient.h"

namespace BML::Interop::Generated::Bml::Events {
inline constexpr const char ApiId[] = "bml.events";
inline constexpr unsigned int Major = 1;
inline constexpr unsigned int Minor = 0;
inline constexpr std::uint64_t Hash = 0x671750F9EA1C375CULL;

inline constexpr BML_InteropFieldDescriptor EventFields[] = {
    {1, "kind", BML_INTEROP_FIELD_INT, 0},
    {2, "filename", BML_INTEROP_FIELD_STRING, 1},
    {3, "is_map", BML_INTEROP_FIELD_BOOL, 1},
    {4, "master_name", BML_INTEROP_FIELD_STRING, 1},
    {5, "filter_class", BML_INTEROP_FIELD_INT, 1},
    {6, "add_to_scene", BML_INTEROP_FIELD_BOOL, 1},
    {7, "reuse_meshes", BML_INTEROP_FIELD_BOOL, 1},
    {8, "reuse_materials", BML_INTEROP_FIELD_BOOL, 1},
    {9, "dynamic", BML_INTEROP_FIELD_BOOL, 1},
    {10, "object_ids", BML_INTEROP_FIELD_OBJECT_ARRAY, 1},
    {11, "master_object", BML_INTEROP_FIELD_OBJECT, 1},
    {12, "script", BML_INTEROP_FIELD_OBJECT, 1},
    {13, "target", BML_INTEROP_FIELD_OBJECT, 1},
    {14, "fixed", BML_INTEROP_FIELD_BOOL, 1},
    {15, "friction", BML_INTEROP_FIELD_FLOAT, 1},
    {16, "elasticity", BML_INTEROP_FIELD_FLOAT, 1},
    {17, "mass", BML_INTEROP_FIELD_FLOAT, 1},
    {18, "collision_group", BML_INTEROP_FIELD_STRING, 1},
    {19, "start_frozen", BML_INTEROP_FIELD_BOOL, 1},
    {20, "enable_collision", BML_INTEROP_FIELD_BOOL, 1},
    {21, "auto_calculate_mass_center", BML_INTEROP_FIELD_BOOL, 1},
    {22, "linear_damp", BML_INTEROP_FIELD_FLOAT, 1},
    {23, "rot_damp", BML_INTEROP_FIELD_FLOAT, 1},
    {24, "collision_surface", BML_INTEROP_FIELD_STRING, 1},
    {25, "mass_center", BML_INTEROP_FIELD_VEC3, 1},
    {26, "convex_meshes", BML_INTEROP_FIELD_OBJECT_ARRAY, 1},
    {27, "ball_centers", BML_INTEROP_FIELD_VEC3_ARRAY, 1},
    {28, "ball_radii", BML_INTEROP_FIELD_FLOAT_ARRAY, 1},
    {29, "concave_meshes", BML_INTEROP_FIELD_OBJECT_ARRAY, 1},
    {30, "command", BML_INTEROP_FIELD_STRING, 1},
    {31, "command_args", BML_INTEROP_FIELD_STRING_ARRAY, 1},
    {32, "config_category", BML_INTEROP_FIELD_STRING, 1},
    {33, "config_key", BML_INTEROP_FIELD_STRING, 1},
    {34, "config_type", BML_INTEROP_FIELD_INT, 1},
    {35, "config_value", BML_INTEROP_FIELD_STRING, 1},
    {36, "cheat_enabled", BML_INTEROP_FIELD_BOOL, 1},
};
inline constexpr BML_InteropSchemaDescriptor Event = {1, "event", EventFields, 36};

inline constexpr BML_InteropSchemaDescriptor Schemas[] = {
    Event,
};

inline constexpr BML_InteropEndpointDescriptor Endpoints[] = {
    {"all", BML_INTEROP_ENDPOINT_STREAM, 0, 1, 0},
};

inline constexpr BML_InteropApiDescriptor Descriptor = {
    sizeof(BML_InteropApiDescriptor), ApiId, Major, Minor, Hash,
    Schemas, sizeof(Schemas) / sizeof(Schemas[0]),
    Endpoints, sizeof(Endpoints) / sizeof(Endpoints[0]),
    nullptr, 0,
};

/* Header-only helpers: still no C++ symbol crosses the DLL boundary. */
inline int Require() {
    return BML_Interop_RequireApi(ApiId, Major, Hash);
}
inline int RegisterProvider(const BML_InteropProviderCallbacks *callbacks, void *userdata) {
    return BML_Interop_RegisterProvider(&Descriptor, callbacks, userdata);
}

namespace EventField {
inline constexpr std::uint32_t Kind = 1;
inline constexpr std::uint32_t Filename = 2;
inline constexpr std::uint32_t IsMap = 3;
inline constexpr std::uint32_t MasterName = 4;
inline constexpr std::uint32_t FilterClass = 5;
inline constexpr std::uint32_t AddToScene = 6;
inline constexpr std::uint32_t ReuseMeshes = 7;
inline constexpr std::uint32_t ReuseMaterials = 8;
inline constexpr std::uint32_t Dynamic = 9;
inline constexpr std::uint32_t ObjectIds = 10;
inline constexpr std::uint32_t MasterObject = 11;
inline constexpr std::uint32_t Script = 12;
inline constexpr std::uint32_t Target = 13;
inline constexpr std::uint32_t Fixed = 14;
inline constexpr std::uint32_t Friction = 15;
inline constexpr std::uint32_t Elasticity = 16;
inline constexpr std::uint32_t Mass = 17;
inline constexpr std::uint32_t CollisionGroup = 18;
inline constexpr std::uint32_t StartFrozen = 19;
inline constexpr std::uint32_t EnableCollision = 20;
inline constexpr std::uint32_t AutoCalculateMassCenter = 21;
inline constexpr std::uint32_t LinearDamp = 22;
inline constexpr std::uint32_t RotDamp = 23;
inline constexpr std::uint32_t CollisionSurface = 24;
inline constexpr std::uint32_t MassCenter = 25;
inline constexpr std::uint32_t ConvexMeshes = 26;
inline constexpr std::uint32_t BallCenters = 27;
inline constexpr std::uint32_t BallRadii = 28;
inline constexpr std::uint32_t ConcaveMeshes = 29;
inline constexpr std::uint32_t Command = 30;
inline constexpr std::uint32_t CommandArgs = 31;
inline constexpr std::uint32_t ConfigCategory = 32;
inline constexpr std::uint32_t ConfigKey = 33;
inline constexpr std::uint32_t ConfigType = 34;
inline constexpr std::uint32_t ConfigValue = 35;
inline constexpr std::uint32_t CheatEnabled = 36;
}

/*
 * Typed consumer bindings are generated from the same descriptor. They
 * remain inline and use only the fixed C ABI through InteropClient.h.
 */

struct EventValue {
    int Kind{};
    bool HasFilename = false;
    std::string Filename{};
    bool HasIsMap = false;
    bool IsMap{};
    bool HasMasterName = false;
    std::string MasterName{};
    bool HasFilterClass = false;
    int FilterClass{};
    bool HasAddToScene = false;
    bool AddToScene{};
    bool HasReuseMeshes = false;
    bool ReuseMeshes{};
    bool HasReuseMaterials = false;
    bool ReuseMaterials{};
    bool HasDynamic = false;
    bool Dynamic{};
    bool HasObjectIds = false;
    std::vector<::BML::Interop::ObjectRef> ObjectIds{};
    bool HasMasterObject = false;
    ::BML::Interop::ObjectRef MasterObject{};
    bool HasScript = false;
    ::BML::Interop::ObjectRef Script{};
    bool HasTarget = false;
    ::BML::Interop::ObjectRef Target{};
    bool HasFixed = false;
    bool Fixed{};
    bool HasFriction = false;
    float Friction{};
    bool HasElasticity = false;
    float Elasticity{};
    bool HasMass = false;
    float Mass{};
    bool HasCollisionGroup = false;
    std::string CollisionGroup{};
    bool HasStartFrozen = false;
    bool StartFrozen{};
    bool HasEnableCollision = false;
    bool EnableCollision{};
    bool HasAutoCalculateMassCenter = false;
    bool AutoCalculateMassCenter{};
    bool HasLinearDamp = false;
    float LinearDamp{};
    bool HasRotDamp = false;
    float RotDamp{};
    bool HasCollisionSurface = false;
    std::string CollisionSurface{};
    bool HasMassCenter = false;
    ::BML::Interop::Vec3 MassCenter{};
    bool HasConvexMeshes = false;
    std::vector<::BML::Interop::ObjectRef> ConvexMeshes{};
    bool HasBallCenters = false;
    std::vector<::BML::Interop::Vec3> BallCenters{};
    bool HasBallRadii = false;
    std::vector<float> BallRadii{};
    bool HasConcaveMeshes = false;
    std::vector<::BML::Interop::ObjectRef> ConcaveMeshes{};
    bool HasCommand = false;
    std::string Command{};
    bool HasCommandArgs = false;
    std::vector<std::string> CommandArgs{};
    bool HasConfigCategory = false;
    std::string ConfigCategory{};
    bool HasConfigKey = false;
    std::string ConfigKey{};
    bool HasConfigType = false;
    int ConfigType{};
    bool HasConfigValue = false;
    std::string ConfigValue{};
    bool HasCheatEnabled = false;
    bool CheatEnabled{};
};

inline int DecodeEvent(const ::BML::Interop::Record &record, EventValue &out) {
    std::uint32_t schema = 0;
    int status = record.Schema(schema);
    if (status != BML_OK)
        return status;
    if (schema != Event.Id)
        return BML_ERROR_INTEROP_SCHEMA_MISMATCH;
    EventValue decoded{};
    if (status == BML_OK) status = record.GetInt(EventField::Kind, decoded.Kind);
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::Filename, decoded.Filename);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasFilename = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasFilename = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::IsMap, decoded.IsMap);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasIsMap = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasIsMap = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::MasterName, decoded.MasterName);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasMasterName = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasMasterName = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetInt(EventField::FilterClass, decoded.FilterClass);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasFilterClass = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasFilterClass = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::AddToScene, decoded.AddToScene);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasAddToScene = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasAddToScene = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::ReuseMeshes, decoded.ReuseMeshes);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasReuseMeshes = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasReuseMeshes = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::ReuseMaterials, decoded.ReuseMaterials);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasReuseMaterials = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasReuseMaterials = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::Dynamic, decoded.Dynamic);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasDynamic = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasDynamic = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetObjectArray(EventField::ObjectIds, decoded.ObjectIds);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasObjectIds = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasObjectIds = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetObjectRef(EventField::MasterObject, decoded.MasterObject);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasMasterObject = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasMasterObject = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetObjectRef(EventField::Script, decoded.Script);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasScript = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasScript = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetObjectRef(EventField::Target, decoded.Target);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasTarget = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasTarget = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::Fixed, decoded.Fixed);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasFixed = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasFixed = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetFloat(EventField::Friction, decoded.Friction);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasFriction = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasFriction = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetFloat(EventField::Elasticity, decoded.Elasticity);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasElasticity = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasElasticity = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetFloat(EventField::Mass, decoded.Mass);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasMass = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasMass = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::CollisionGroup, decoded.CollisionGroup);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasCollisionGroup = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasCollisionGroup = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::StartFrozen, decoded.StartFrozen);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasStartFrozen = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasStartFrozen = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::EnableCollision, decoded.EnableCollision);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasEnableCollision = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasEnableCollision = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::AutoCalculateMassCenter, decoded.AutoCalculateMassCenter);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasAutoCalculateMassCenter = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasAutoCalculateMassCenter = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetFloat(EventField::LinearDamp, decoded.LinearDamp);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasLinearDamp = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasLinearDamp = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetFloat(EventField::RotDamp, decoded.RotDamp);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasRotDamp = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasRotDamp = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::CollisionSurface, decoded.CollisionSurface);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasCollisionSurface = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasCollisionSurface = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetVec3(EventField::MassCenter, decoded.MassCenter);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasMassCenter = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasMassCenter = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetObjectArray(EventField::ConvexMeshes, decoded.ConvexMeshes);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasConvexMeshes = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasConvexMeshes = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetVec3Array(EventField::BallCenters, decoded.BallCenters);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasBallCenters = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasBallCenters = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetFloatArray(EventField::BallRadii, decoded.BallRadii);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasBallRadii = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasBallRadii = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetObjectArray(EventField::ConcaveMeshes, decoded.ConcaveMeshes);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasConcaveMeshes = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasConcaveMeshes = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::Command, decoded.Command);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasCommand = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasCommand = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetStringArray(EventField::CommandArgs, decoded.CommandArgs);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasCommandArgs = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasCommandArgs = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::ConfigCategory, decoded.ConfigCategory);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasConfigCategory = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasConfigCategory = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::ConfigKey, decoded.ConfigKey);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasConfigKey = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasConfigKey = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetInt(EventField::ConfigType, decoded.ConfigType);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasConfigType = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasConfigType = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetString(EventField::ConfigValue, decoded.ConfigValue);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasConfigValue = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasConfigValue = true;
        }
    }
    if (status == BML_OK) {
        const int fieldStatus = record.GetBool(EventField::CheatEnabled, decoded.CheatEnabled);
        if (fieldStatus == BML_ERROR_NOT_FOUND) {
            decoded.HasCheatEnabled = false;
        } else if (fieldStatus != BML_OK) {
            status = fieldStatus;
        } else {
            decoded.HasCheatEnabled = true;
        }
    }
    if (status == BML_OK)
        out = std::move(decoded);
    return status;
}

inline int OpenAll(int capacity, ::BML::Interop::Stream &out) {
    const int status = Require();
    return status == BML_OK ? ::BML::Interop::OpenStream(ApiId, "all", capacity, out) : status;
}

} // namespace BML::Interop::Generated::Bml::Events

#else
#define BML_INTEROP_BML_EVENTS_API_ID "bml.events"
#define BML_INTEROP_BML_EVENTS_MAJOR 1u
#define BML_INTEROP_BML_EVENTS_MINOR 0u
#define BML_INTEROP_BML_EVENTS_HASH UINT64_C(0x671750F9EA1C375C)

static const BML_InteropFieldDescriptor BML_InteropGenerated_Bml_Events_EventFields[] = {
    {1u, "kind", BML_INTEROP_FIELD_INT, 0},
    {2u, "filename", BML_INTEROP_FIELD_STRING, 1},
    {3u, "is_map", BML_INTEROP_FIELD_BOOL, 1},
    {4u, "master_name", BML_INTEROP_FIELD_STRING, 1},
    {5u, "filter_class", BML_INTEROP_FIELD_INT, 1},
    {6u, "add_to_scene", BML_INTEROP_FIELD_BOOL, 1},
    {7u, "reuse_meshes", BML_INTEROP_FIELD_BOOL, 1},
    {8u, "reuse_materials", BML_INTEROP_FIELD_BOOL, 1},
    {9u, "dynamic", BML_INTEROP_FIELD_BOOL, 1},
    {10u, "object_ids", BML_INTEROP_FIELD_OBJECT_ARRAY, 1},
    {11u, "master_object", BML_INTEROP_FIELD_OBJECT, 1},
    {12u, "script", BML_INTEROP_FIELD_OBJECT, 1},
    {13u, "target", BML_INTEROP_FIELD_OBJECT, 1},
    {14u, "fixed", BML_INTEROP_FIELD_BOOL, 1},
    {15u, "friction", BML_INTEROP_FIELD_FLOAT, 1},
    {16u, "elasticity", BML_INTEROP_FIELD_FLOAT, 1},
    {17u, "mass", BML_INTEROP_FIELD_FLOAT, 1},
    {18u, "collision_group", BML_INTEROP_FIELD_STRING, 1},
    {19u, "start_frozen", BML_INTEROP_FIELD_BOOL, 1},
    {20u, "enable_collision", BML_INTEROP_FIELD_BOOL, 1},
    {21u, "auto_calculate_mass_center", BML_INTEROP_FIELD_BOOL, 1},
    {22u, "linear_damp", BML_INTEROP_FIELD_FLOAT, 1},
    {23u, "rot_damp", BML_INTEROP_FIELD_FLOAT, 1},
    {24u, "collision_surface", BML_INTEROP_FIELD_STRING, 1},
    {25u, "mass_center", BML_INTEROP_FIELD_VEC3, 1},
    {26u, "convex_meshes", BML_INTEROP_FIELD_OBJECT_ARRAY, 1},
    {27u, "ball_centers", BML_INTEROP_FIELD_VEC3_ARRAY, 1},
    {28u, "ball_radii", BML_INTEROP_FIELD_FLOAT_ARRAY, 1},
    {29u, "concave_meshes", BML_INTEROP_FIELD_OBJECT_ARRAY, 1},
    {30u, "command", BML_INTEROP_FIELD_STRING, 1},
    {31u, "command_args", BML_INTEROP_FIELD_STRING_ARRAY, 1},
    {32u, "config_category", BML_INTEROP_FIELD_STRING, 1},
    {33u, "config_key", BML_INTEROP_FIELD_STRING, 1},
    {34u, "config_type", BML_INTEROP_FIELD_INT, 1},
    {35u, "config_value", BML_INTEROP_FIELD_STRING, 1},
    {36u, "cheat_enabled", BML_INTEROP_FIELD_BOOL, 1},
};

static const BML_InteropSchemaDescriptor BML_InteropGenerated_Bml_Events_Schemas[] = {
    {1u, "event", BML_InteropGenerated_Bml_Events_EventFields, 36u},
};

static const BML_InteropEndpointDescriptor BML_InteropGenerated_Bml_Events_Endpoints[] = {
    {"all", BML_INTEROP_ENDPOINT_STREAM, 0u, 1u, 0},
};

static const BML_InteropApiDescriptor BML_InteropGenerated_Bml_Events_Descriptor = {
    sizeof(BML_InteropApiDescriptor), BML_INTEROP_BML_EVENTS_API_ID, BML_INTEROP_BML_EVENTS_MAJOR, BML_INTEROP_BML_EVENTS_MINOR, BML_INTEROP_BML_EVENTS_HASH,
    BML_InteropGenerated_Bml_Events_Schemas, sizeof(BML_InteropGenerated_Bml_Events_Schemas) / sizeof(BML_InteropGenerated_Bml_Events_Schemas[0]),
    BML_InteropGenerated_Bml_Events_Endpoints, sizeof(BML_InteropGenerated_Bml_Events_Endpoints) / sizeof(BML_InteropGenerated_Bml_Events_Endpoints[0]),
    0, 0u,
};
#define BML_INTEROP_BML_EVENTS_DESCRIPTOR BML_InteropGenerated_Bml_Events_Descriptor
#endif // __cplusplus
